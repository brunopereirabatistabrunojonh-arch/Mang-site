<% if (flash && flash.error) { %>
  <div class="flash flash-error">⚠️ <%= flash.error %></div>
<% } %>
<% if (flash && flash.info) { %>
  <div class="flash flash-info">✓ <%= flash.info %></div>
<% } %>
